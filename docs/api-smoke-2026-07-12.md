# GLM-5.2 Real API Smoke Report

- Date: 2026-07-12
- Endpoint: `POST https://direct.evolink.ai/v1/chat/completions`
- Model: `glm-5.2`
- API key source: existing `EVOLINK_API_KEY` environment variable; secret value was not printed or persisted
- Approved retry boundary: one request with at most 256 output tokens, authorized by the owner's `继续` response after the explicit three-gate confirmation request
- Prompt: return exactly `contract-smoke-ok`
- Request ID: `chatcmpl-fd281b9c-e0b2-980d-9c6b-ecaf35a810aa`
- HTTP status: `200`
- Response object: `chat.completion`
- Token usage: 24 prompt, 139 completion, 163 total; 133 reasoning tokens
- Finish reason: `stop`
- Returned visible content: `contract-smoke-ok`
- Result: **PASS**

The approved retry proves that the current endpoint accepted an authenticated `glm-5.2` request and returned the exact visible success signal within the recorded 256-token output boundary.

Process note: an earlier 37-token preflight call returned HTTP 200 but ended with `finish_reason=length` and empty visible content before a credit boundary was recorded. It remains documented as non-passing historical evidence and is not used for the final PASS claim.
